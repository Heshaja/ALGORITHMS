# this below code having time complexity as o(n)

class Solution(object):
    def searchInsert(self, nums, target):
        for i in range(len(nums)):
            if target == nums[i] :
                return i
            
            elif target < nums[i]:
                return i
        return len(nums)


        
